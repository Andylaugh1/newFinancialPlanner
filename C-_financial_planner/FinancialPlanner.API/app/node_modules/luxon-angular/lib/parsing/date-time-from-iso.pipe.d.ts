import { PipeTransform } from '@angular/core';
import { DateTime } from 'luxon';
export declare class DateTimeFromIsoPipe implements PipeTransform {
    transform<T extends string | null | undefined>(value: T): T extends string ? DateTime : null;
}
