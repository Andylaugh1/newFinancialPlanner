import { PipeTransform } from '@angular/core';
import { DateTime } from 'luxon';
export declare class DateTimeFromJsDatePipe implements PipeTransform {
    transform<T extends Date | null | undefined>(value: T): T extends Date ? DateTime : null;
}
