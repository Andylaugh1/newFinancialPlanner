import { PipeTransform } from '@angular/core';
import { Duration } from 'luxon';
export declare class DurationFromMillisecondsPipe implements PipeTransform {
    transform<T extends number | null | undefined>(value: T): T extends number ? Duration : null;
}
