import { PipeTransform } from '@angular/core';
import { DateTime } from 'luxon';
export declare class DateTimeFromFormatPipe implements PipeTransform {
    transform<T extends string | null | undefined>(value: T, format: string): T extends string ? DateTime : null;
}
