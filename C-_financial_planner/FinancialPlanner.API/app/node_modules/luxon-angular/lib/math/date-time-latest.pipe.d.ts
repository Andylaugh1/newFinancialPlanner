import { PipeTransform } from '@angular/core';
import { DateTime } from 'luxon';
export declare class DateTimeLatestPipe implements PipeTransform {
    transform<T extends DateTime[] | null | undefined>(value: T): T extends DateTime[] ? DateTime : null;
}
