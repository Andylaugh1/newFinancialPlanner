import { PipeTransform } from '@angular/core';
import { Duration } from 'luxon';
export declare class DurationShortestPipe implements PipeTransform {
    transform<T extends Duration[] | null | undefined>(value: T): T extends Duration[] ? Duration : null;
}
