import { PipeTransform } from '@angular/core';
import { DateTime } from 'luxon';
export declare class DateTimeToIsoDatePipe implements PipeTransform {
    transform<T extends DateTime | null | undefined>(value: T): T extends DateTime ? string : null;
}
